# Start Deter in Codex

Repository:

```text
fitzss/Deter
```

Branch:

```text
codex/halo-hardware-program
```

Use a Codex environment with internet access enabled for current manufacturer research.

Paste this task:

```text
Read AGENTS.md completely, then execute docs/CODEX_TASK_H0.md end to end.

This repository is the source of truth for the complete Deter/HALO physical engineering program. Do not work from main, create a parallel scaffold, or reduce the task to the HALO Lab web application.

The immediate goal is to close Gate G0 and produce a current, compatibility-checked, founder-ready Gate 1 order sheet for the reference aircraft. Research live official manufacturer facts, challenge the provisional platform decision, update the BOM and mass/power budgets, and state exactly what Fitz should buy now, buy locally, wait on, and not buy.

Do not place an order or claim any physical step has passed. End with the exact founder actions required to approve and place the order, plus the next task after order confirmation.
```

After ordering, give Codex the non-sensitive order details and ask it to update `hardware/inventory.csv` and prepare the receiving/build session from `docs/BUILD_TEST_PROTOCOL.md`.
